services:
  backend:
    build: .
    container_name: bid-backend
    ports:
      - "8000:8000"
    restart: always
    environment:
      # 👇 確保是 AWS RDS 的網址
      - DATABASE_URL=postgresql://postgres:0830allan@database-1.c8bwi0kmyu9k.us-east-1.rds.amazonaws.com:5432/postgres
      
      # 👇 確保是 AWS Valkey 的網址 (Serverless 要用 rediss://)
      - REDIS_URL=rediss://bid-redis-mq2mra.serverless.use1.cache.amazonaws.com:6379
     